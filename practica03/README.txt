Para ejecutar este programa basta con compilarlo usando el comando cc -fopenmp -o QuickSort QuickSort.c y después utilizando ./QuickSort
Los enteros se deben introducir en una línea cada uno, ya que si se ponen varios en una sola línea, si la entrada es muy grande, por una razón que desconozco, el programa parece tardarse mucho o nunca terminar.
